[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    valid: Valid meeting notes (expect 200 with actions or empty list)
    invalid: Invalid/gibberish input (expect 400 or empty actions)
    integration: Tests that require a live Ollama + FastAPI server
    unit: Pure unit tests with no external dependencies
